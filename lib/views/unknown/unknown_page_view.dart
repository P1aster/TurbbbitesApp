import 'package:flutter/material.dart';

class UnknownPageView extends StatelessWidget {
  const UnknownPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page not found"),
    );
  }
}
